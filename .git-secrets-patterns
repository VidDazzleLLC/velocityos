# Git Secrets Patterns for VelocityOS
# This file contains patterns to detect secrets in commits
# Usage: git secrets --add-provider -- cat .git-secrets-patterns

# Firebase CI tokens (format: 1//...)
1//[0-9A-Za-z_-]{30,}

# Firebase API keys (format: AIza...)
AIza[0-9A-Za-z_-]{35}

# Generic private keys
-----BEGIN (RSA |DSA |EC )?PRIVATE KEY-----

# Generic API key patterns
api[_-]?key['\"]?\s*[:=]\s*['\"][a-zA-Z0-9_-]{20,}['\"]
secret[_-]?key['\"]?\s*[:=]\s*['\"][a-zA-Z0-9_-]{20,}['\"]

# Firebase service account emails
.*@.*\.iam\.gserviceaccount\.com

# Generic tokens
(access[_-]?token|auth[_-]?token)['\"]?\s*[:=]\s*['\"][a-zA-Z0-9_-]{20,}['\"]

# Generic passwords in code
password['\"]?\s*[:=]\s*['\"][^'\"]{8,}['\"]

# AWS keys
(A3T[A-Z0-9]|AKIA|AGPA|AIDA|AROA|AIPA|ANPA|ANVA|ASIA)[A-Z0-9]{16}

# Generic secret environment variables
(SECRET|PASSWORD|TOKEN|KEY|CREDENTIALS)=[^#\n]{20,}
